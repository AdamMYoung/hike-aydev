"use client";

import { FellDTO } from "database";

type PinProps = {
  isCompleted?: boolean;
  coordinates: [number, number];
};

export const Pin = (_: PinProps) => {
  return null;
};

Pin.displayName = "Pin";
